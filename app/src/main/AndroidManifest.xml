<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cis.finalprojectapplication" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".RentListActivity" >
        </activity>
        <activity android:name=".AddToUnReturnActivity" />
        <activity android:name=".ShowAllActivity" />
        <activity android:name=".ShowDetailActivity" />
        <activity android:name=".AddInstruActivity" />
        <activity android:name=".ShowActivity" />
        <activity android:name=".ReturnActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>